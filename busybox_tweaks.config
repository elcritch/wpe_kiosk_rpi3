
CONFIG_ADJTIMEX=y
CONFIG_RDATE=y
CONFIG_HWCLOCK=y
# Set hwclock to use /var/lib/hwclock/adjtime instead of /etc/adjtime 
# seems nerves would like the /var option better
# note: /var has symlinks for /var/lib/misc -> tmp but not /var/lib FIXME
CONFIG_FEATURE_HWCLOCK_ADJTIME_FHS=y

